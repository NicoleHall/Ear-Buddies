<div class="container">
  <div class="newbox">
    <% @playlists.each do |playlist| %>
      <%= link_to "#{playlist.subject}", playlist_path(playlist) %><br>
    <% end %>
  </div>
</div>


<!-- {:controller => "playlists", :action => "selector", :playlist => playlist.id } -->
