
<div class="container">
  <div class="newbox">
    <% @playlist.podcasts.each do |podcast| %>

    <%= podcast.ep_title %><br>
    <%= podcast.url %><br>
    <%= button_to "Remove Podcast From Playlist", podcast_path(podcast.id), method: :delete %><br>

    <% end %>
  </div>
</div>
